pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('tests') {
            steps {
                sh "mvn clean & install -f 005 - Recording Test Results and Artifacts/02 - Java Unit Tests"
            }
        }
    }
}
