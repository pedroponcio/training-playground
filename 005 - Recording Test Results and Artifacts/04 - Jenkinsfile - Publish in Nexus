pipeline {
    agent { 
		docker { 
			image 'maven:3.3.3'
			args '--net="training-playground"'
		}
	}
    stages {
        stage('tests') {
            steps {
                sh "mvn clean "
            }
        }
    }
}
